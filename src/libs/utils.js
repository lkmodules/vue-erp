import KmLayout from '@/layout/km-layout.vue'
/**
 * @param {Ayyay} menuList
 * @param {Object} routerObj
 */
export const buildRoutesByMenu = (menuList, routerObj) => {
  console.log(menuList)
  console.log(routerObj)
  let routes = []
  
  return routes
}